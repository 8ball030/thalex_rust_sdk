{
  "TopOfBookPerpetual": {
    "type": "array",
    "description": "Top of book data, `null` for resolutions other than 1 minute",
    "minItems": 4,
    "maxItems": 4,
    "prefixItems": [
      {
        "type": "number",
        "description": "Top bid price, or `null` if bid order book is empty.",
        "nullable": true
      },
      {
        "type": "number",
        "description": "Top bid size, or `null` if bid order book is empty.",
        "nullable": true
      },
      {
        "type": "number",
        "description": "Top ask price, or `null` if ask order book is empty.",
        "nullable": true
      },
      {
        "type": "number",
        "description": "Top ask size, or `null` if ask order book is empty.",
        "nullable": true
      }
    ]
  },
  "PerpetualDataPoint": {
    "type": "array",
    "title": "Time, mark, funding payment, top bid, top ask",
    "description": "A single OHLC data point with funding payment and top of book",
    "minItems": 7,
    "maxItems": 7,
    "prefixItems": [
      {
        "type": "number",
        "description": "Time (Unix timestamp) of the start of the data point."
      },
      {
        "type": "number",
        "description": "Open (first) value of the data point."
      },
      {
        "type": "number",
        "description": "High (max) value of the data point."
      },
      {
        "type": "number",
        "description": "Low (min) value of the data point."
      },
      {
        "type": "number",
        "description": "Close (last) value of the data point."
      },
      {
        "type": "number",
        "description": "Funding payment during the interval represented by the data point."
      },
      {
        "$ref": "#/components/schemas/TopOfBookPerpetual"
      }
    ]
  },
  "PerpetualMarkPriceHistoricalData": {
    "type": "array",
    "description": "Array of PerpetualDataPoint items",
    "items": {
      "$ref": "#/components/schemas/PerpetualDataPoint"
    }
  }
}
      