/*
 * Thalex API Description
 *
 * Talex API documentation
 *
 * The version of the OpenAPI document: 2.49.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// Success39ResultMark : Array of mark price data points. Each mark price data point is an array of mark price data in OHLC format followed by an optional array of top of book data. Top of book data is returned only for `1m` resolution and set to `null` otherwise.  Note that the inner array format depends on the `instrument_type` flag.  Note also that the top bid and top ask data can be set to `null` for the `1m` resolution if there was no quote at the start of the correspondent minute interval. 
/// Array of mark price data points. Each mark price data point is an array of mark price data in OHLC format followed by an optional array of top of book data. Top of book data is returned only for `1m` resolution and set to `null` otherwise.  Note that the inner array format depends on the `instrument_type` flag.  Note also that the top bid and top ask data can be set to `null` for the `1m` resolution if there was no quote at the start of the correspondent minute interval. 
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
#[serde(untagged)]
pub enum Success39ResultMark {
    Array(Vec<Vec<serde_json::Value>>),
    Array(Vec<Vec<serde_json::Value>>),
    Array(Vec<Vec<serde_json::Value>>),
}

impl Default for Success39ResultMark {
    fn default() -> Self {
        Self::Array(Default::default())
    }
}

